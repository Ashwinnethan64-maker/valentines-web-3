{
    "name": "valentine-love-generator",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd client && npm install && cd ../server && npm install",
        "build": "cd client && npm run build && cd ../server && npm run build"
    },
    "engines": {
        "node": ">=18"
    },
    "dependencies": {
        "@prisma/client": "^7.3.0",
        "dotenv": "^17.2.4"
    },
    "devDependencies": {
        "@types/node": "^25.2.3"
    }
}
